<div class="hero-banner">
    <div class="hero-text">
        <span class="badge">🚀 OCR • AQA • Edexcel • CIE</span>
        <h1>Computer Science Explained <br><span class="highlight">The Way It Should Be</span></h1>
        <p>Clear trace tables, step-by-step pseudocode, and exam-style questions—minus the dry textbook fluff.</p>
    </div>
</div>

<style>    
    .hero-banner {
        background: var(--bg-card);
        border: var(--border-mega);
        box-shadow: var(--shadow-hard);
        border-radius: var(--radius-cartoon);
        padding: 3rem 2.5rem;
        margin-bottom: 2.5rem;
    }

    .hero-text {
        max-width: 680px;
    }

    .badge {
        display: inline-block;
        font-family: var(--font-code);
        font-weight: 700;
        font-size: 0.8rem;
        padding: 0.25rem 0.65rem;
        border: var(--border-thick);
        border-radius: var(--radius-pill);
        background: var(--accent-lime);
        box-shadow: 2px 2px 0px var(--color-black);
        margin-bottom: 1.2rem;
    }

    .hero-banner h1 {
        font-family: var(--font-heading);
        font-size: 2.4rem;
        line-height: 1.2;
        margin-bottom: 1rem;
        letter-spacing: -0.02em;
    }

    .highlight {
        background: var(--accent-cyan);
        padding: 0.05em 0.25em;
        border: var(--border-thick);
        box-shadow: 3px 3px 0px var(--color-black);
        display: inline;
    }

    .hero-banner p {
        font-size: 1.1rem;
        line-height: 1.5;
        color: #334155;
        font-weight: 500;
    }
</style>